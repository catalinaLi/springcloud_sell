spring:
  application:
    name: order #name
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config #Eureka Config's name
      profile: dev #profiles
#management:
#  endpoints:
#    web:
#      exposure:
#        include: bus-refresh